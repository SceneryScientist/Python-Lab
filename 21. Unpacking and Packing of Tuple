t = () 
while True: 
  print() 
  menu = int(input(‘ 1. Create tuple\n 2. Modify or delete element using unpack/pack\n
  3. Modify or delete element elements using list constructor\n 4. Exit\n Enter:’)) 
   
  if menu==1: 
    for i in range(4): 
      ch = eval(input(‘Enter element:’)) 
      t += (ch,) 
     print(t) 
   
  elif menu==2: 
    print() 
    menu1 = int(input(‘ 1. Modify entry\n 2. Delete entry\n Enter:’)) 
    a,b,c,d = t 
    if menu1==1: 
      index = int(input(‘Enter the index of element to modify:’)) 
      value = eval(input(‘Enter value to replace it with:’)) 
      if index==0: 
        a = value 
      elif index==1: 
        b = value 
      elif index==2: 
        c = value 
      elif index==3: 
        d = value 
      else: 
        print (‘Invalid input.’) 
      t = a,b,c,d 

    elif menu1==2: 
      index = int(input(‘Enter the index of element to delete:’)) 
      if index==0: 
        del(a) 
        t = b,c,d 
      elif index==1: 
        del(b) 
        t = a,c,d 
      elif index==2: 
        del(c) 
        t = a,b,d 
      elif index==3: 
        del(d) 
        t = a,b,c 
      else: 
        print (‘Invalid input.’) 
        print(t) 

    elif menu==3: 
      print() 
      menu2 = int(input(‘ 1. Modify entry\n 2. Delete entry\n Enter:’)) 
      l = list(t) 
      if menu2==1: 
        index = int(input(‘Enter the index of element to modify:’)) 
        value = eval(input(‘Enter value to replace:’)) 
        l[index] = value 
        t = tuple(l) 

      elif menu2==2: 
        index = int(input(‘Enter the index of element to delete:’)) 
        value = l[index] 
        l.remove(value) 
        t = tuple(l) 
        print(t) 
     
    elif menu==4: 
      break 
     
    else: 
      print (‘Invalid entry.’) 

